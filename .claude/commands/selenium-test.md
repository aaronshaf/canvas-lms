---
description: Self-test the selenium skill suite against a small fixture directory. Run this after updating any selenium skill to verify the pipeline works before using it on real data. Flags: --fast (no Docker), --dir <path>, --layer <n>, --reset-baseline.
---

Invoke the `selenium-skills-test` skill with this argument: $ARGUMENTS
