---
description: Run the full selenium trim pipeline (audit → trim –apply → coverage compare) on a spec/selenium directory. Short names like `courses` are resolved to `spec/selenium/courses` automatically.
---

Invoke the `selenium-pipeline` skill with this argument: $ARGUMENTS
