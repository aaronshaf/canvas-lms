<% @routes.each do |route| %>
  <% route_path = route.path.spec.to_s.sub("(.:format)", "")
     next if /\.json$/.match?(route_path) %>
  <h3 class='endpoint'>
    <%= route.verb %> <%= route_path %>
  </h3>
  <div>
    <strong>Scope: </strong>
    <code class="scope"><%= TokenScopesHelper.scope_from_route(route) %></code>
  </div>
<% end %>
