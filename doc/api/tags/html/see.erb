<% if object.has_tag?(:see) %>
  <p class="see_tag_title"><strong>See Also:</strong></p>
  <ul class="see">
    <% object.tags(:see).each do |tag| %>
      <li><%= linkify(tag.name, tag.text) %></li>
    <% end %>
  </ul>
<% end %>
